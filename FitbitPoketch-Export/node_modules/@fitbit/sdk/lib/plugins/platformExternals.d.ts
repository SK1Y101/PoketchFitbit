import { Plugin } from 'rollup';
import { ComponentType } from '../componentTargets';
export declare const externals: {
    device: string[];
    companion: string[];
    settings: string[];
};
export declare function plugin(component: ComponentType): Plugin;
