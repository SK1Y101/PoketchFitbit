/// <reference types="node" />
import { Transform } from 'stream';
import ProjectConfiguration from './ProjectConfiguration';
export declare function makeDeviceManifest({ projectConfig, buildId, targetDevice, }: {
    projectConfig: ProjectConfiguration;
    buildId: string;
    targetDevice: string;
}): Transform;
export declare function makeCompanionManifest({ projectConfig, hasSettings, buildId, }: {
    projectConfig: ProjectConfiguration;
    hasSettings: boolean;
    buildId: string;
}): Transform;
